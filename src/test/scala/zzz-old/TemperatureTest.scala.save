
import types._
import mathx._
import Scalar._
import units._

object TemperatureTest {

    def main(args: Array[String]) {

        println(unitList)

        val TF = 100 * degF
        val TC = convertDegFtoC(TF)
        val TK = convertDegFtoK(TF)

        println(TC/degC)
        println(TK/degK)

        println
        }}  
        
